{
    "compilerOptions": {
        // qual modelo eles estao utilizando 
        "target": "es6",
        // será gerado a pasta JS, dentro de app
        "outDir": "app/js",
        // enquanto houver um erro no arquivo TS, não será gerado o arquivo js
        "noEmitOnError": true,
        // o TS não irá adotar nenhum tipo (não vai deixar a variável sem declaração adotar o tipo any)
        "noImplicitAny": true
    },
    // uso da pasta TS, iremos utilizar essa pasta para criar a pasta JS
    "include": [
        "app/ts/**/*"
    ]
}